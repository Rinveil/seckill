server:
  port: 8084
spring:
  application:
    name: seckill-order

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
