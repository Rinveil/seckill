server:
  port: 8082
spring:
  application:
    name: seckill-activity

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
