server:
  port: 8083
spring:
  application:
    name: seckill-core

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
