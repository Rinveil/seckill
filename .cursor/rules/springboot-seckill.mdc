---
description: SpringBoot 秒杀项目编码约束（JDK17 / 高并发 / 最小改动）
alwaysApply: true
---

# SpringBoot 秒杀项目编码约束

1. 遵循 Java Spring Boot 规范，使用 JDK 17、Maven，使用 Lombok；数据库访问统一使用 **MyBatis-Plus**。
2. 高并发场景优先考虑：缓存、分布式锁、防超卖、限流、幂等。
3. 修改代码遵循最小改动原则，不要一次性大范围重构；改动后输出关键变更说明；**每完成 roadmap 一步即 git commit 并 push 到远程**。
4. 使用 Redis 做库存预扣、RabbitMQ 异步削峰时，要考虑消息丢失、重复消费。
5. SQL 禁止大 offset 分页，禁止循环数据库查询；优先使用连接池。
6. 输出代码之后，主动提示潜在并发风险点。
7. 终端执行 `mvn`、压测命令，必须向我确认，禁止自动执行高危命令。
8. 不要修改 `application` 密钥配置，密钥读取使用环境变量。
9. 给出设计时，先输出简要架构思路，再写代码，不要直接扔一堆文件。
